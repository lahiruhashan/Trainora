////  NotificationRow.swift//  Trainora////  Created by user266021 on 5/6/25.//import SwiftUIstruct NotificationRow: View {    let notification: AppNotification    var body: some View {        HStack(alignment: .top, spacing: 12) {            icon                .frame(width: 40, height: 40)                .background(                    notification.isSeen                        ? Color.gray.opacity(0.2) : Color.blue.opacity(0.15)                )                .cornerRadius(10)            VStack(alignment: .leading, spacing: 4) {                Text(notification.title)                    .font(.headline)                    .foregroundColor(notification.isSeen ? .primary : .blue)                Text(notification.body)                    .font(.subheadline)                    .foregroundColor(.secondary)                Text(                    notification.date.formatted(                        date: .abbreviated,                        time: .shortened                    )                )                .font(.caption)                .foregroundColor(.gray)            }            Spacer()            if !notification.isSeen {                Circle()                    .fill(Color.blue)                    .frame(width: 10, height: 10)            }        }        .padding(.vertical, 8)    }    private var icon: some View {        let imageName: String        switch notification.type {        case .reminder: imageName = "bell.fill"        case .dietBreakfast: imageName = "sunrise.fill"        case .dietLunch: imageName = "fork.knife"        case .dietDinner: imageName = "moon.fill"        case .inactivity: imageName = "figure.walk"        case .appUpdate: imageName = "arrow.down.app.fill"        }        return Image(systemName: imageName)            .resizable()            .scaledToFit()            .foregroundColor(.blue)            .padding(6)    }}