////  ExerciseCategoryCardView.swift//  Trainora////  Created by user266021 on 5/1/25.//import SwiftUIstruct ExerciseCategoryCardView: View {    let category: ExerciseCategory    let onFavoriteTapped: () -> Void    var body: some View {        HStack(alignment: .top, spacing: 12) {            Image(uiImage: UIImage(named: category.imageName) ?? UIImage(systemName: "photo")!)                .resizable()                .scaledToFill()                .frame(width: 60, height: 60)                .clipShape(RoundedRectangle(cornerRadius: 12))            VStack(alignment: .leading, spacing: 6) {                HStack {                    Text(category.title)                        .font(.headline)                    Spacer()                    Button(action: onFavoriteTapped) {                        Image(systemName: category.isFavorite ? "heart.fill" : "heart")                            .foregroundColor(.red)                    }                }                Text("Exercises: \(category.totalExercises)")                    .font(.subheadline)                    .foregroundColor(.secondary)                Text("🔥 \(Double(category.totalCalories).roundedToTwoDecimals) kcal • ⏱ \(Double(category.totalDuration).roundedToTwoDecimals) min")                    .font(.footnote)                    .foregroundColor(.secondary)            }        }        .padding()        .background(Color(.systemBackground))        .cornerRadius(12)        .shadow(color: Color.black.opacity(0.05), radius: 4, x: 0, y: 2)    }}